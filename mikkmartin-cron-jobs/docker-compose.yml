version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: mikkmartin-cron-jobs_server_1
      APP_PORT: 4000
    volumes:
      - ${APP_DATA_DIR}/data:/data

  server:
    init: true
    image: ghcr.io/mikkmartin/cron-app:main@sha256:54c03bb45e2839cc1d592ef9599fa0e69421627cecebb7e3758dc4cb63caba3a
    restart: on-failure
